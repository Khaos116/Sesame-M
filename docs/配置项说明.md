# 配置项说明（按模块）

> 本文件由 `docs/tools/gen_fields.ps1` 从源码中的字段声明自动生成，顺序与源码声明一致（与配置页显示顺序基本一致）。
> 修改源码配置项后重新生成：`powershell -ExecutionPolicy Bypass -File docs/tools/gen_fields.ps1`
> 分组与模型顺序取自 `ModelGroup` / `ModelOrder`。
>
> - 「默认 / 范围」里的枚举常量（如 `DonationType.ZERO`）是源码里的取值名，对应配置页下拉框的选项。
> - 「依赖」列表示该项依赖的开关，父开关关闭时该项不生效。
> - 同一模型内字段顺序与配置页一致；不同分组的模型排列顺序由 `ModelOrder` 决定。

## 基础 | 基础 (BaseModel)

| 配置项 (code) | 显示名称 | 默认 / 范围 | 依赖 |
| --- | --- | --- | --- |
| `stayAwake` | 保持唤醒 | 默认 true |  |
| `checkInterval` | 执行间隔(分钟) | 默认 50，范围 1~720 |  |
| `execAtTimeList` | 定时执行(关闭:-1) | 默认 065530, 2359, 24 |  |
| `wakenAtTimeList` | 定时唤醒(关闭:-1) | 默认 0650, 2350 |  |
| `energyTime` | 只收能量时间(范围) | 默认 0700-0731 |  |
| `timedTaskModel` | 定时任务模式 | 默认 TimedTaskModel.SYSTEM |  |
| `timeoutRestart` | 超时重启 | 默认 true |  |
| `waitWhenException` | 异常等待时间(分钟) | 默认 60，范围 0~1440 |  |
| `backupConfigDays` | 按天和修改备份配置保存数(滚动覆盖) | 默认 5 |  |
| `rpcRequestList` | RPC请求列表及每日执行数(慎用) | 默认 空 |  |
| `rpcRequestTaskList` | RPC可选任务列表(长按列表中的项仅移除用，内容需打开rpcResquest.json文件配置) | 默认 空 |  |
| `puzzleMaxAttempts` | 拼图验证每个窗口最多自动尝试次数(1-5，计算错了会重试) | 默认 4，范围 1~5 |  |

## 全局配置 (AppConfig，不分账号)

> 下列项目保存在全局 `appConfig.json`，在「配置 → 模块功能」里修改；`gen_fields.ps1` 不扫描这些普通字段。

| 配置项 (code) | 显示名称 | 默认 / 范围 |
| --- | --- | --- |
| `newRpc` | 使用新接口 | 默认 true |
| `showToast` | 气泡提示 | 默认 true |
| `toastOffsetY` | 气泡纵向偏移 | 默认 0 |
| `enableOnGoing` | 开启状态栏禁删 | 默认 false |
| `closeCaptchaDialog` | 屏蔽部分弹窗 | 默认 true |
| `autoPuzzleSlider` | 自动处理拼图滑块验证 | 默认 true；旧账号配置首次使用时迁移 |
| `newPuzzleSlider` | 使用新版拼图识别 | 默认 true；关闭后使用原有 M 版逻辑 |

## 森林 | 森林 (AntForestV2)

| 配置项 (code) | 显示名称 | 默认 / 范围 | 依赖 |
| --- | --- | --- | --- |
| `collectEnergy` | 收集能量 | 默认 false |  |
| `findEnergyCollect` | 找能量 | 默认 false | `collectEnergy` |
| `dontCollectList` | 不收取能量列表 | 默认 空 |  |
| `batchRobEnergy` | 一键收取 | 默认 false |  |
| `CollectSelfEnergyType` | 收单个能量球 \| 方式 | 默认 CollectSelfType.ALL |  |
| `CollectSelfEnergyThreshold` | 收单个能量球阈值(0不限制) | 默认 0，范围 0~10000 |  |
| `pkEnergy` | Pk榜收取 \| 开关 | 默认 false |  |
| `energyPvp` | 1V1能量挑战 \| 开关 | 默认 false |  |
| `collectWateringBubble` | 收取金球 | 默认 false |  |
| `wateredFriendList` | 统计 \| 应被好友浇水 | 默认 空 |  |
| `collectRobExpandEnergyEnable` | 倍卡额外能量 | 默认 true |  |
| `collectRobExpandEnergy` | 倍卡额外能量(大于该值收取) | 默认 0，范围 0~1000000 | `collectRobExpandEnergyEnable` |
| `expiredEnergy` | 收取过期能量 | 默认 false |  |
| `queryInterval` | 查询间隔(毫秒或毫秒范围) | 默认 500-1000 |  |
| `collectInterval` | 收取间隔(毫秒或毫秒范围) | 默认 1000-1500 |  |
| `doubleCollectInterval` | 双击间隔(毫秒或毫秒范围) | 默认 50-150 |  |
| `balanceNetworkDelay` | 平衡网络延迟 | 默认 true |  |
| `advanceTime` | 提前时间(毫秒) | 默认 0，范围 0~500 |  |
| `tryCount` | 尝试收取(次数) | 默认 1，范围 0~10 |  |
| `retryInterval` | 重试间隔(毫秒) | 默认 1000，范围 0~10000 |  |
| `drawGameCenterAward` | 森林乐园 \| 游戏宝箱 | 默认 true |  |
| `CollectBombEnergyLimit` | 单个炸弹能量大于该值收取 | 默认 0，范围 0~100000 |  |
| `continuousUseCardOptions` | 连续兑换使用道具卡片 \| 选项 | 默认 空 |  |
| `autoUseShieldCard` | 自动续用保护罩 | 默认 false |  |
| `continuousUseShieldHour` | 自动续用保护罩(小时) | 默认 24，范围 1~168 | `autoUseShieldCard` |
| `returnWater` | 返水 \| 开启 | 默认 false |  |
| `returnWater10` | 返水 \| 10克需收能量(0不限) | 默认 10 | `returnWater` |
| `returnWater18` | 返水 \| 18克需收能量(0不限) | 默认 18 | `returnWater` |
| `returnWater33` | 返水 \| 33克需收能量(0不限) | 默认 33 | `returnWater` |
| `waterFriendType` | 浇水 \| 动作 | 默认 WaterFriendType.WATER_00 |  |
| `waterFriendList` | 浇水 \| 好友列表 | 默认 空 |  |
| `waterFriendEnergySendChat` | 浇水 \| 发送已浇水提醒 | 默认 false |  |
| `waterFriendEnergyFirst` | 浇水 \| 每次首先执行 | 默认 false |  |
| `doubleWaterFriendEnergy` | 浇水 \| 强制检查重复一次 | 默认 false |  |
| `helpFriendCollectType` | 复活能量 \| 动作 | 默认 HelpFriendCollectType.NONE |  |
| `helpFriendCollectList` | 复活能量 \| 好友列表 | 默认 空 |  |
| `helpFriendCollectListLimit` | 复活好友能量下限(大于该值复活,0不限制) | 默认 0，范围 0~100000 | `helpFriendCollectType` |
| `vitalityExchangeBenefit` | 活力值 \| 兑换权益 | 默认 false |  |
| `vitality_ExchangeBenefitList` | 活力值 \| 权益列表 | 默认 空 | `vitalityExchangeBenefit` |
| `whackModeName` | 6秒拼手速 \| 运行模式 | 默认 whackModeNames.CLOSE |  |
| `whackModeGames` | 6秒拼手速 \| 激进模式局数 | 默认 5 | `whackModeName` |
| `whackModeCount` | 6秒拼手速 \| 兼容模式击打数 | 默认 15 | `whackModeName` |
| `earliestwhackMoleTime` | 6秒拼手速 \| 最早执行(24小时制) | 默认 8，范围 0~23 | `whackModeName` |
| `collectProp` | 收集道具 | 默认 false |  |
| `whoYouWantToGiveTo` | 赠送道具好友列表 | 默认 空 |  |
| `energyRain` | 收集能量雨 | 默认 false |  |
| `giveEnergyRainList` | 赠送能量雨好友列表 | 默认 空 |  |
| `useEnergyRainLimit` | 兑换使用限时能量雨卡 | 默认 false |  |
| `userPatrol` | 旧版保护地巡护 | 默认 false |  |
| `monopolyPatrol` | 新版保护地 \| 自动前进 | 默认 false |  |
| `monopolyTasks` | 新版保护地 \| 自动任务 | 默认 false |  |
| `monopolyAnimalEnergy` | 新版动物伙伴 \| 领取能量 | 默认 false |  |
| `monopolyDispatchAnimal` | 新版动物伙伴 \| 自动派遣 | 默认 false |  |
| `monopolyDispatchPriority` | 新版动物伙伴 \| 派遣优先级 | 默认 0 | `monopolyDispatchAnimal` |
| `AutoMonopolyTaskList` | 新版保护地 \| 自动黑名单 | 默认 true | `monopolyTasks` |
| `MonopolyTaskList` | 新版保护地 \| 黑名单列表 | 默认 空 | `AutoMonopolyTaskList` |
| `consumeAnimalPropType` | 派遣动物伙伴 | 默认 ConsumeAnimalPropType.NONE |  |
| `receiveForestTaskAward` | 森林任务 | 默认 false |  |
| `AutoAntForestVitalityTaskList` | 活力值 \| 自动黑名单 | 默认 true |  |
| `AntForestVitalityTaskList` | 活力值 \| 黑名单列表 | 默认 空 | `AutoAntForestVitalityTaskList` |
| `collectGiftBox` | 领取礼盒 | 默认 false |  |
| `medicalHealth` | 医疗健康 | 默认 false |  |
| `greenLife` | 森林集市 | 默认 false |  |
| `greenRent` | 绿色租赁 | 默认 false |  |
| `youthPrivilege` | 青春特权 \| 森林道具 | 默认 false |  |
| `ecoLife` | 绿色行动 \| 开启 | 默认 false |  |
| `ecoLifeOptions` | 绿色行动 \| 选项 | 默认 空 | `ecoLife` |
| `partnerteamWater` | 组队合种浇水 | 默认 false |  |
| `partnerteamWaterNum` | 组队合种浇水(g) | 默认 10，范围 10~5000 | `partnerteamWater` |
| `loveteamWater` | 真爱合种浇水 | 默认 false |  |
| `loveteamWaterNum` | 真爱合种浇水(g) | 默认 20，范围 20~10000 | `loveteamWater` |
| `ForestHunt` | 森林寻宝 | 默认 false |  |
| `AutoAntForestHuntTaskList` | 抽抽乐任务 \| 自动黑名单 | 默认 true | `ForestHunt` |
| `AntForestHuntTaskList` | 抽抽乐任务 \| 黑名单列表 | 默认 空 | `AutoAntForestHuntTaskList` |
| `ForestHuntDraw` | 森林寻宝抽奖 | 默认 false | `ForestHunt` |
| `ForestHuntHelp` | 森林寻宝助力 | 默认 false | `ForestHunt` |
| `NORMALForestHuntHelp` | 普通场景强制助力(助力任务不在列表中时使用，如果日志显示失效请关闭) | 默认 false | `ForestHunt` |
| `ACTIVITYForestHuntHelp` | 活动场景强制助力(同上) | 默认 false | `ForestHunt` |
| `ForestHuntHelpList` | 点击配置寻宝助力列表(填写shareId中开头的22-24位字符在\"4O7FEYDgn\"前的) | 默认 空 | `ForestHunt` |
| `dress` | 装扮保护 \| 开启 | 默认 false |  |
| `dressDetailList` | 装扮保护 \| 装扮信息 | 空 | `dress` |
| `dressDetailListClear` | 装扮保护 \| 装扮信息清除 | 默认 () -> dressDetailList.reset() | `dress` |

## 庄园 | 庄园 (AntFarm)

| 配置项 (code) | 显示名称 | 默认 / 范围 | 依赖 |
| --- | --- | --- | --- |
| `receiveFarmTaskAward` | 饲料任务及奖励 | 默认 false |  |
| `AutoAntFarmDoFarmTaskList` | 庄园饲料 \| 自动黑名单 | 默认 true | `receiveFarmTaskAward` |
| `AntFarmDoFarmTaskList` | 庄园饲料 \| 黑名单列表 | 默认 空 | `AutoAntFarmDoFarmTaskList` |
| `useNewEggTool` | 新蛋卡 \| 使用 | 默认 false |  |
| `useAccelerateTool` | 加速卡 \| 使用 | 默认 false |  |
| `useAccelerateToolOptions` | 加速卡 \| 选项 | 默认 空 | `useAccelerateTool` |
| `useBigEaterTool` | 加饭卡 \| 使用 | 默认 false |  |
| `useSpecialFood` | 特殊食品 \| 使用 | 默认 false |  |
| `useSpecialFoodCountLimit` | 特殊食品 \| 使用上限(无限:0) | 默认 0 | `useSpecialFood` |
| `rewardFriend` | 打赏好友 | 默认 false |  |
| `recallAnimalType` | 召回小鸡 | 默认 RecallAnimalType.ALWAYS |  |
| `feedAnimal` | 投喂小鸡 | 默认 false |  |
| `feedFriendAnimal` | 帮喂小鸡 \| 开启 | 默认 true |  |
| `feedFriendAnimalList` | 帮喂小鸡 \| 好友列表 | 默认 空 | `feedFriendAnimal` |
| `hireAnimalType` | 雇佣小鸡 \| 动作 | 默认 HireAnimalType.NONE |  |
| `hireAnimalList` | 雇佣小鸡 \| 好友列表 | 默认 空 | `hireAnimalType` |
| `sendBackAnimalWay` | 遣返小鸡 \| 方式 | 默认 SendBackAnimalWay.NORMAL |  |
| `sendBackAnimalType` | 遣返小鸡 \| 动作 | 默认 SendBackAnimalType.NONE |  |
| `sendFriendList` | 遣返小鸡 \| 好友列表 | 默认 空 | `sendBackAnimalType` |
| `notifyFriendType` | 通知赶鸡 \| 动作 | 默认 NotifyFriendType.NONE |  |
| `notifyFriendList` | 通知赶鸡 \| 好友列表 | 默认 空 | `notifyFriendType` |
| `ornamentsDressUp` | 装扮焕新 \| 开启 | 默认 false |  |
| `ornamentsDressUpList` | 装扮焕新 \| 套装列表 | 默认 空 | `ornamentsDressUp` |
| `ornamentsDressUpDays` | 装扮焕新 \| 焕新频率(天) | 默认 7，范围 1~30 | `ornamentsDressUp` |
| `drawMachine` | 装扮抽抽乐 | 默认 false |  |
| `AutoAntFarmDrawMachineTaskList` | 抽抽乐 \| 自动黑名单 | 默认 true | `drawMachine` |
| `AntFarmDrawMachineTaskList` | 抽抽乐 \| 黑名单列表 | 默认 空 | `AutoAntFarmDrawMachineTaskList` |
| `IPexchangeBenefit` | 抽抽乐兑换 \| 开启 | 默认 false |  |
| `donationType` | 每日捐蛋 \| 方式 | 默认 DonationType.ZERO |  |
| `donationAmount` | 每日捐蛋 \| 倍数(每项) | 默认 1 | `donationType` |
| `competition` | 爱心鸡结号 \| 开启 | 默认 false |  |
| `competitionReceiveTask` | 爱心鸡结号 \| 领取奖励 | 默认 false | `competition` |
| `competitionDonate` | 爱心鸡结号 \| 自动捐蛋 | 默认 false | `competition` |
| `competitionDonateAmount` | 爱心鸡结号 \| 自动捐蛋数量 | 默认 5，范围 0~1000 | `competitionDonate` |
| `competitionStealRank` | 爱心鸡结号 \| 偷榜 | 默认 false | `competition` |
| `competitionStealMinutes` | 爱心鸡结号 \| 偷榜提前分钟数 | 默认 30，范围 0~240 | `competitionStealRank` |
| `family` | 亲密家庭 \| 开启 | 默认 false |  |
| `familyOptions` | 亲密家庭 \| 选项 | 默认 空 | `family` |
| `notInviteList` | 亲密家庭 \| 不邀请列表 | 默认 空 | `family` |
| `enableSleep` | 小鸡起床 \| 自动起床（自动睡觉已禁用） | 默认 false |  |
| `sleepTime` | 小鸡起床 \| 入睡参考时间 | 默认 2001 | `enableSleep` |
| `sleepMinutes` | 小鸡起床 \| 睡眠时长(分钟) | 默认 590，范围 1~600 | `enableSleep` |
| `recordFarmGame` | 小鸡乐园 \| 游戏改分(星星球、登山赛、飞行赛、揍小鸡) | 默认 false |  |
| `farmGameTime` | 小鸡乐园 \| 游戏时间(范围) | 默认 farmGameTimeList | `recordFarmGame` |
| `drawGameCenterAward` | 小鸡乐园 \| 游戏宝箱 | 默认 false |  |
| `gameCenterBuyMallItem` | 小鸡乐园 \| 乐园集市 | 默认 false |  |
| `gameCenterBuyMallItemList` | 小鸡乐园 \| 兑奖 | 默认 空 | `gameCenterBuyMallItem` |
| `kitchen` | 小鸡厨房 | 默认 false |  |
| `chickenDiary` | 小鸡日记 | 默认 false |  |
| `harvestProduce` | 收取爱心鸡蛋 | 默认 false |  |
| `receiveFarmToolReward` | 收取道具奖励 | 默认 false |  |
| `acceptGift` | 收麦子 | 默认 false |  |
| `visitFriendList` | 送麦子 \| 好友列表 | 默认 空 |  |

## 新村 | 新村 (AntStall)

| 配置项 (code) | 显示名称 | 默认 / 范围 | 依赖 |
| --- | --- | --- | --- |
| `openShopType` | 摆摊 \| 动作 | 默认 OpenShopType.NONE |  |
| `openShopList` | 摆摊 \| 好友列表 | 默认 空 | `openShopType` |
| `closeShop` | 收摊 \| 开启 | 默认 false |  |
| `closeShopTime` | 收摊 \| 摆摊时长(分钟) | 默认 120，范围 1~1440 | `closeShop` |
| `pasteTicketType` | 贴罚单 \| 动作 | 默认 PasteTicketType.NONE |  |
| `pasteTicketList` | 贴罚单 \| 好友列表 | 默认 空 | `pasteTicketType` |
| `throwManureType` | 丢肥料 \| 动作 | 默认 ThrowManureType.NONE |  |
| `throwManureList` | 丢肥料 \| 好友列表 | 默认 空 | `throwManureType` |
| `manualCollectManure` | 收肥料 \| 手动收取 | 默认 false |  |
| `sendBackShop` | 请走小摊 \| 开启 | 默认 false |  |
| `sendBackShopTime` | 请走小摊 \| 允许摆摊时长(分钟) | 默认 121 | `sendBackShop` |
| `sendBackShopWhiteList` | 请走小摊 \| 白名单(超时也不赶) | 默认 空 | `sendBackShop` |
| `sendBackShopBlackList` | 请走小摊 \| 黑名单(不超时也赶) | 默认 空 | `sendBackShop` |
| `inviteOpenShopType` | 邀请摆摊 \| 动作 | 默认 InviteOpenShopType.NONE |  |
| `inviteOpenShopList` | 邀请摆摊 \| 好友列表 | 默认 空 | `inviteOpenShopType` |
| `taskList` | 新村任务 \| 加速产币 | 默认 false |  |
| `AutoAntStallTaskList` | 新村任务 \| 自动黑名单 | 默认 true | `taskList` |
| `AntStallTaskList` | 新村任务 \| 黑名单列表 | 默认 空 | `AutoAntStallTaskList` |
| `doTaskOnce` | 新村任务仅执行一次 | 默认 false | `taskList` |
| `donate` | 助力就业岗位 | 默认 false |  |
| `nextVillage` | 解锁新村新店 | 默认 false |  |
| `inviteRegister` | 邀请开通 \| 开启 | 默认 false |  |
| `inviteRegisterList` | 邀请开通 \| 好友列表 | 默认 空 | `inviteRegister` |
| `assistFriend` | 分享助力 \| 开启 | 默认 false |  |
| `assistFriendList` | 分享助力 \| 好友列表 | 默认 空 | `assistFriend` |

## 农场 | 农场 (AntOrchard)

| 配置项 (code) | 显示名称 | 默认 / 范围 | 依赖 |
| --- | --- | --- | --- |
| `executeInterval` | 执行间隔(毫秒) | 默认 500，范围 500~null |  |
| `orchardListTask` | 农场任务 | 默认 false |  |
| `AutoAntOrchardTaskList` | 农场任务 \| 自动黑名单 | 默认 true | `orchardListTask` |
| `AntOrchardTaskList` | 农场任务 \| 黑名单列表 | 默认 空 | `AutoAntOrchardTaskList` |
| `orchardSpreadManure` | 农场施肥 \| 开启 | 默认 false |  |
| `useBatchSpread` | 一键施肥5次(边界突破) | 默认 false | `orchardSpreadManure` |
| `orchardSpreadManureSceneList` | 农场施肥 \| 场景列表 | 默认 空 | `orchardSpreadManure` |
| `drawGameCenterAward` | 农场乐园 \| 游戏宝箱 | 默认 true |  |
| `assistFriend` | 分享助力 \| 开启 | 默认 false |  |
| `assistFriendList` | 分享助力 \| 好友列表 | 默认 空 | `assistFriend` |

## 森林 | 保护 (ProtectEcology)

| 配置项 (code) | 显示名称 | 默认 / 范围 | 依赖 |
| --- | --- | --- | --- |
| `cooperateWater` | 合种 \| 浇水 | 默认 false |  |
| `cooperateWaterList` | 合种 \| 日浇水量列表 | 默认 空 | `cooperateWater` |
| `cooperateWaterTotalLimitList` | 合种 \| 总浇水量列表 | 默认 空 | `cooperateWater` |
| `protectMarathonType` | 碳中和 \| 马拉松 | 默认 ProtectType.NONE |  |
| `protectMarathonList` | 碳中和 \| 马拉松列表 | 默认 空 | `protectMarathonType` |
| `protectNewAncientTreeType` | 碳中和 \| 古树医生 | 默认 ProtectType.NONE |  |
| `protectNewAncientTreeList` | 碳中和 \| 古树医生列表 | 默认 空 | `protectNewAncientTreeType` |
| `protectTree` | 保护森林 \| 植树 | 默认 false |  |
| `protectTreeList` | 保护森林 \| 植树列表 | 默认 空 | `protectTree` |
| `protectReserve` | 保护动物 \| 保护地 | 默认 false |  |
| `reserveList` | 保护动物 \| 保护地列表 | 默认 空 | `protectReserve` |
| `protectReserveMinNum` | 保护地 \| 最少保护 | 默认 false |  |
| `protectReserveNum` | 保护地 \|最少保护下限 | 默认 1 | `protectReserveMinNum` |
| `protectAnimal` | 保护动物 \| 护林员 | 默认 false |  |
| `protectAnimalList` | 保护动物 \| 护林员列表 | 默认 空 | `protectAnimal` |
| `protectBeachMinNum` | 保护海洋 \| 单个海滩保护 | 默认 false |  |
| `protectBeachNum` | 保护海洋 \|海滩保护下限 | 默认 1 | `protectBeachMinNum` |
| `protectBeach` | 保护海洋 \| 海滩 | 默认 false |  |
| `protectOceanList` | 保护海洋 \| 海滩列表 | 默认 空 | `protectBeach` |

## 森林 | 物种 (AntDodo)

| 配置项 (code) | 显示名称 | 默认 / 范围 | 依赖 |
| --- | --- | --- | --- |
| `dodoTaskList` | 物种任务 | 默认 false |  |
| `AutoAntDodoTaskList` | 物种任务 \| 自动黑名单 | 默认 true | `dodoTaskList` |
| `AntDodoTaskList` | 物种任务 \| 黑名单列表 | 默认 空 | `AutoAntDodoTaskList` |
| `useProp` | 使用道具 \| 开启 | 默认 false |  |
| `usePropList` | 使用道具 \| 道具列表 | 默认 空 | `useProp` |
| `useCollectTimingType` | 抽卡道具 \| 使用时机 | 默认 TimingType.EVERY_DAY | `useProp` |
| `useUniversalCardBookStatusType` | 万能卡片 \| 图鉴状态类型 | 默认 BookStatusType.END | `useProp` |
| `useUniversalCardBookCollectedStatusType` | 万能卡片 \| 图鉴收集状态 | 默认 BookCollectedStatusType.ALL | `useProp` |
| `useUniversalCardBookCollectedhasCollected` | 万能卡片 \| 优先兑换未获得状态卡片 | 默认 false | `useProp` |
| `useUniversalCardMedalGenerationStatusType` | 万能卡片 \| 勋章合成状态 | 默认 MedalGenerationStatusType.ALL | `useProp` |
| `useUniversalCardFantasticLevelType` | 万能卡片 \| 最低等级 | 默认 FantasticLevelType.MAGIC | `useProp` |
| `bookMedal` | 图鉴勋章 \| 开启 | 默认 false |  |
| `bookMedalOptions` | 图鉴勋章 \| 选项 | 默认 空 | `bookMedal` |
| `collectToFriendType` | 帮抽卡片 \| 动作 | 默认 CollectToFriendType.NONE |  |
| `collectToFriendList` | 帮抽卡片 \| 好友列表 | 默认 空 | `collectToFriendType` |
| `giftToFriend` | 赠送卡片 \| 开启 | 默认 false |  |
| `giftToFriendBookStatusType` | 赠送卡片 \| 图鉴状态类型 | 默认 BookStatusType.ALL | `giftToFriend` |
| `giftToFriendBookCollectedStatusType` | 赠送卡片 \| 图鉴收集状态 | 默认 BookCollectedStatusType.ALL | `giftToFriend` |
| `giftToFriendMedalGenerationStatusType` | 赠送卡片 \| 勋章合成状态 | 默认 MedalGenerationStatusType.ALL | `giftToFriend` |
| `giftToFriendFantasticLevelType` | 赠送卡片 \| 最低等级 | 默认 FantasticLevelType.COMMON | `giftToFriend` |
| `giftToFriendList` | 赠送卡片 \| 好友列表 | 默认 空 | `giftToFriend` |

## 森林 | 海洋 (AntOcean)

| 配置项 (code) | 显示名称 | 默认 / 范围 | 依赖 |
| --- | --- | --- | --- |
| `queryTaskList` | 海洋任务 | 默认 false |  |
| `AutoAntOceanAntiepTaskList` | 海洋任务 \| 自动黑名单 | 默认 true | `queryTaskList` |
| `AntOceanAntiepTaskList` | 海洋任务 \| 黑名单列表 | 默认 空 | `AutoAntOceanAntiepTaskList` |
| `cleanOceanType` | 清理海域 \| 动作 | 默认 CleanOceanType.NONE |  |
| `cleanOceanList` | 清理海域 \| 好友列表 | 默认 空 | `cleanOceanType` |
| `exchangeUniversalPiece` | 万能拼图 \| 制作 | 默认 false |  |
| `useUniversalPiece` | 万能拼图 \| 使用 | 默认 false |  |
| `replica` | 潘多拉海域 | 默认 false |  |
| `antfishEnable` | 海洋摸鱼 \| 开启摸鱼 | 默认 false |  |
| `antfishAutoTask` | 海洋摸鱼 \| 摸鱼任务 | 默认 false | `antfishEnable` |
| `AutoAntOceanFishBlackList` | 海洋摸鱼 \| 自动黑名单 | 默认 true | `antfishAutoTask` |
| `AntOceanFishBlackList` | 摸鱼任务 \| 黑名单列表 | 默认 空 | `AutoAntOceanFishBlackList` |

## 运动 | 运动 (AntSports)

| 配置项 (code) | 显示名称 | 默认 / 范围 | 依赖 |
| --- | --- | --- | --- |
| `walk` | 行走路线 \| 开启 | 默认 false |  |
| `PathThemeMapList` | 行走路线 \| 路线主题 | 默认 0 | `walk` |
| `walkMinimumCompleteCount` | 全主题路线(选最少完成数) \| 开启 | 默认 false | `walk` |
| `sportsTasks` | 运动任务 | 默认 false |  |
| `AutoAntSportsTaskList` | 运动任务 \| 自动黑名单 | 默认 true | `sportsTasks` |
| `AntSportsTaskList` | 运动任务 \| 黑名单列表 | 默认 空 | `AutoAntSportsTaskList` |
| `receiveCoinAsset` | 收运动币 | 默认 false |  |
| `club` | 抢好友 \| 开启 | 默认 false |  |
| `clubTrainItemType` | 抢好友 \| 训练动作 | 默认 TrainItemType.NONE | `club` |
| `clubTradeMemberType` | 抢好友 \| 抢购动作 | 默认 TradeMemberType.NONE | `club` |
| `clubTradeMemberList` | 抢好友 \| 好友列表 | 默认 空 | `club` |
| `tiyubiz` | 文体中心 | 默认 false |  |
| `syncStepCount` | 同步步数 \| 自定义 | 默认 22000，范围 0~78000 |  |
| `earliestSyncStepTime` | 同步步数 \| 最早同步时间(24小时制) | 默认 0，范围 0~23 |  |
| `latestExchangeTime` | 行走捐 \| 最晚捐步时间(24小时制) | 默认 22 |  |
| `minExchangeCount` | 行走捐 \| 最小捐步步数 | 默认 10 |  |
| `neverLand` | 健康岛 \| 开启 | 默认 false |  |
| `QUERY_SIGN` | 健康岛 \| 每日签到 | 默认 false | `neverLand` |
| `QUERY_TASK_CENTER` | 健康岛 \| 做任务 加能量 | 默认 false | `neverLand` |
| `QUERY_BUBBLE_TASK` | 健康岛 \| 领取能量球奖励 | 默认 false | `neverLand` |
| `WALK_GRID` | 健康岛 \| 能量泵 | 默认 false | `neverLand` |
| `WALK_GRID_MAX` | 健康岛 \| 单次执行能量泵最大次数(不限:0) | 默认 5 | `neverLand` |
| `WALK_GRID_LIMIT` | 健康岛 \| 使用能量泵剩余能量值(低于该值停止使用) | 默认 10000 | `neverLand` |
| `MapListSwitch` | 健康岛 \| 自动切岛 | 默认 false | `neverLand` |
| `awardspecialActivityReceive` | 健康岛 \| 领取活动岛奖励 | 默认 false | `neverLand` |

## 会员 | 会员 (AntMember)

| 配置项 (code) | 显示名称 | 默认 / 范围 | 依赖 |
| --- | --- | --- | --- |
| `AntMemberTask` | 会员任务 | 默认 false |  |
| `AutoAntMemberTaskList` | 会员任务 \| 自动黑名单 | 默认 true | `AntMemberTask` |
| `AntMemberTaskList` | 会员任务 \| 黑名单列表 | 默认 空 | `AutoAntMemberTaskList` |
| `memberSign` | 会员签到 | 默认 false |  |
| `memberPointExchangeBenefit` | 会员积分 \| 兑换权益 | 默认 false |  |
| `memberPointExchangeBenefitList` | 会员积分 \| 权益列表 | 默认 空 | `memberPointExchangeBenefit` |
| `collectSesame` | 芝麻粒 \| 领取 | 默认 false |  |
| `AutoMemberCreditSesameTaskList` | 芝麻粒任务 \| 自动黑名单 | 默认 true | `collectSesame` |
| `MemberCreditSesameTaskList` | 芝麻粒任务 \| 黑名单列表 | 默认 空 | `AutoMemberCreditSesameTaskList` |
| `SesameGrowthBehavior` | 攒芝麻分进度 | 默认 false |  |
| `enableGameCenter` | 游戏中心 \| 得乐园豆 | 默认 false |  |
| `KuaiDiFuLiJia` | 我的快递 \| 福利加 | 默认 false |  |

## 其他 | 经营 (GreenFinance)

| 配置项 (code) | 显示名称 | 默认 / 范围 | 依赖 |
| --- | --- | --- | --- |
| `greenFinanceLsxd` | 打卡 \| 绿色行动 | 默认 false |  |
| `greenFinanceLscg` | 打卡 \| 绿色采购 | 默认 false |  |
| `greenFinanceLsbg` | 打卡 \| 绿色办公 | 默认 false |  |
| `greenFinanceWdxd` | 打卡 \| 绿色销售 | 默认 false |  |
| `greenFinanceLswl` | 打卡 \| 绿色物流 | 默认 false |  |
| `greenFinancePointFriend` | 收取 \| 好友金币 | 默认 false |  |
| `greenFinanceDonation` | 捐助 \| 快过期金币 | 默认 false |  |

## 金豆 | 金豆夺宝 (goldenbeans)

| 配置项 (code) | 显示名称 | 默认 / 范围 | 依赖 |
| --- | --- | --- | --- |
| `goldenBeansVisitGarden` | 签到、任务、矿工与乐园奖励 | 默认 false |  |
| `goldenBeansAutoTask` | 自动完成任务与领奖 | 默认 false |  |
| `goldenBeansSign` | 自动签到 | 默认 false |  |
| `goldenBeansGameDraw` | 金豆乐园奖励(抽奖/游戏) | 默认 false |  |
| `goldenBeansAutoMine` | 自动挖矿 | 默认 false |  |
| `goldenBeansCollectReward` | 领奖后数据同步 | 默认 false |  |
| `AutoGoldenBeansTaskList` | 金豆夺宝 \| 自动黑名单 | 默认 true |  |
| `GoldenBeansTaskList` | 金豆夺宝 \| 黑名单列表 | 默认 空 | `AutoGoldenBeansTaskList` |
| `goldenBeansAutoManureExchange` | 金豆夺宝 \| 自动肥料换豆 | 默认 false |  |
| `goldenBeansManureExchangeLimit` | 金豆夺宝 \| 肥料换豆单日上限(0不限) | 默认 0，范围 0~null | `goldenBeansAutoManureExchange` |
| `goldenBeansAutoSesameExchange` | 金豆夺宝 \| 自动芝麻粒换豆 | 默认 false |  |
| `goldenBeansSesameExchangeLimit` | 金豆夺宝 \| 芝麻粒换豆单日上限(0不限) | 默认 0，范围 0~null | `goldenBeansAutoSesameExchange` |
| `executeInterval` | 操作间隔(毫秒) | 默认 500，范围 500~null |  |

## 其他 | AI答 (AnswerAI)

| 配置项 (code) | 显示名称 | 默认 / 范围 | 依赖 |
| --- | --- | --- | --- |
| `useGeminiAI` | AI类型 | 默认 AIType.CUSTOM |  |
| `useGeminiAIToken` | GeminiAI \| 设置令牌 | 空 |  |
| `customAIUrl` | 自定义AI \| 接口地址(根地址,如/v1) | 空 |  |
| `customAIModel` | 自定义AI \| 模型名 | 空 |  |
| `customAIKey` | 自定义AI \| 令牌 | 空 |  |
| `customAIMaxTokens` | 自定义AI \| 输出Token上限(0=不发) | 默认 1024，范围 0~8192 |  |
| `customAITest` | AI答题 \| 测试响应 | 默认 this::testConnection |  |

## 其他 | 鱼塘 (FishTask)

| 配置项 (code) | 显示名称 | 默认 / 范围 | 依赖 |
| --- | --- | --- | --- |

##  | 理财稳当当签到 (DayDaySave)

| 配置项 (code) | 显示名称 | 默认 / 范围 | 依赖 |
| --- | --- | --- | --- |
| `checkIn` | 每日签到 | 默认 false |  |

##  | 好运卡任务状态 (LuckCardStatus)

| 配置项 (code) | 显示名称 | 默认 / 范围 | 依赖 |
| --- | --- | --- | --- |
| `inspect` | 查询任务状态 | 默认 false |  |
| `triggerTasks` | 执行明确好运卡任务 | 默认 false |  |

##  | 支真假题目查询 (FactCheckRewards)

| 配置项 (code) | 显示名称 | 默认 / 范围 | 依赖 |
| --- | --- | --- | --- |
| `inspect` | 查询答题任务 | 默认 false |  |

##  | 森林绿植活动 (ForestPlantRewards)

| 配置项 (code) | 显示名称 | 默认 / 范围 | 依赖 |
| --- | --- | --- | --- |
| `inspect` | 查询绿植活动 | 默认 false |  |

##  | 天天赚现金 (DailyCash)

| 配置项 (code) | 显示名称 | 默认 / 范围 | 依赖 |
| --- | --- | --- | --- |
| `checkIn` | 每日签到 | 默认 false |  |
| `receiveCash` | 累计满5元领取(每日最多一次) | 默认 false |  |

##  | 实体红包任务 (PromoprodRewards)

| 配置项 (code) | 显示名称 | 默认 / 范围 | 依赖 |
| --- | --- | --- | --- |
| `inspect` | 查询任务状态 | 默认 false |  |
| `claimCompleted` | 领取服务端已完成奖励 | 默认 false |  |

##  | 开门红理财节现金 (WealthDayRewards)

| 配置项 (code) | 显示名称 | 默认 / 范围 | 依赖 |
| --- | --- | --- | --- |
| `collectCash` | 领取可提取现金 | 默认 false |  |
| `maxCashFen` | 单次领取上限(分) | 默认 500，范围 1~100000 |  |

## 会员 | 黄金票每周福利 (WeeklyWelfare)

| 配置项 (code) | 显示名称 | 默认 / 范围 | 依赖 |
| --- | --- | --- | --- |
| `signIn` | 每日签到 | 默认 false |  |
| `weeklyPrize` | 第七日签到后领取周奖励 | 默认 false |  |

##  | 健康岛红包碎片兑换 (HealthIslandRewards)

| 配置项 (code) | 显示名称 | 默认 / 范围 | 依赖 |
| --- | --- | --- | --- |
| `exchange` | 兑换即将到期的碎片 | 默认 false |  |
| `prizeId` | 指定兑换礼品ID | 空 |  |
| `maxPieces` | 单次最多消耗碎片 | 默认 500，范围 1~100000 |  |
| `inspectStages` | 查询地图阶段奖励 | 默认 false |  |
| `claimStages` | 领取已达成阶段奖励 | 默认 false |  |

##  | 网商银行福利签到 (MyBankWelfare)

| 配置项 (code) | 显示名称 | 默认 / 范围 | 依赖 |
| --- | --- | --- | --- |
| `signIn` | 会员签到 | 默认 false |  |
| `inspectPoints` | 查询福利金及到期情况 | 默认 false |  |
| `inspectCoupons` | 查询福利券 | 默认 false |  |
| `useCoupon` | 使用已有福利券(每日最多一次) | 默认 false |  |
| `inspectBenefits` | 查询可用福利金权益 | 默认 false |  |
| `useBenefits` | 使用已有福利金权益 | 默认 false |  |
| `maxBenefits` | 单次使用权益数量上限 | 默认 10，范围 1~20 |  |

##  | 网商福利金签到 (MyBankBenefitSignIn)

| 配置项 (code) | 显示名称 | 默认 / 范围 | 依赖 |
| --- | --- | --- | --- |
| `signIn` | 领取福利金签到奖励 | 默认 false |  |

## 其他 | 其他任务 (OtherTask)

| 配置项 (code) | 显示名称 | 默认 / 范围 | 依赖 |
| --- | --- | --- | --- |
| `haojiaWuyou` | 好家无忧卡 | 默认 false |  |

